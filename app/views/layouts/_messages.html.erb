<div id='messages'>
  <% flash.each do |key, value| %>
    <% if key != 'timedout' %>
      <% next if (value == 'You need to sign in or sign up before continuing.' && key == 'alert' && params['controller'] == 'devise/sessions' && params['action'] == 'new') %>
      <div class="alert alert-<%= key %>">
        <a href="#" data-dismiss="alert" class="close">×</a>
        <%= value.html_safe %>
      </div>
    <% end %>
  <% end %>
</div>
